* {
    box-sizing: border-box;
    font-family: 'Manrope', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

body {
    margin: 0;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    background: radial-gradient(circle at top, #2e026d, #0b021f 60%); /* 💜 Nouveau dégradé violet sombre à la place du bleu nuit */
    color: #f9f9fb; /* 💜 Couleur de texte plus claire */
}

.game-topbar {
    background: linear-gradient(140deg, #3a0ca3, #240046); /* 💜 Dégradé violet profond */
    border-bottom: 1px solid rgba(216, 180, 254, 0.25); /* 💜 Bordure lilas */
    box-shadow: 0 14px 28px -18px rgba(76, 29, 149, 0.8); /* 💜 Ombre violette */
}

.game-topbar .top-nav {
    max-width: min(1080px, 94vw);
    margin: 0 auto;
}

.game-topbar .nav-links a {
    color: rgba(250, 245, 255, 0.9); /* 💜 Texte plus clair */
}

.game-topbar .nav-links .active {
    border-color: #c084fc; /* 💜 Accent violet clair pour le lien actif */
}

.game-wrapper {
    width: min(960px, 92vw);
    padding: 2.5rem 2rem 2rem;
    background: rgba(46, 16, 101, 0.85); /* 💜 Fond semi-transparent violet foncé */
    border: 1px solid rgba(216, 180, 254, 0.25); /* 💜 Bordure lilas douce */
    border-radius: 20px;
    box-shadow: 0 25px 50px -12px rgba(67, 0, 128, 0.6); /* 💜 Ombre adoucie violette */
    margin: 2.5rem auto;
}

.game-header {
    text-align: center;
    margin-bottom: 2rem;
}

.game-header h1 {
    margin: 0 0 0.5rem;
    font-size: clamp(2rem, 5vw, 2.75rem);
    color: #f5e0ff; /* 💜 Titre plus lumineux */
}

.game-header p {
    margin: 0;
    color: #e9d5ff; /* 💜 Sous-texte plus doux */
}

.game-area {
    display: grid;
    gap: 1rem;
    justify-items: center;
    position: relative;
}

#pong {
    width: 100%;
    max-width: 760px;
    background: linear-gradient(135deg, #5b21b6, #3b0764); /* 💜 Nouveau dégradé violet profond */
    border: 2px solid rgba(216, 180, 254, 0.4); /* 💜 Bordure lilas */
    border-radius: 12px;
}

.quiz-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
    background: rgba(10, 1, 23, 0.8); /* 💜 Fond plus sombre et légèrement teinté violet */
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.25s ease;
}

.quiz-overlay.visible {
    opacity: 1;
    pointer-events: auto;
}

.quiz-card {
    width: min(520px, 100%);
    background: radial-gradient(circle at top, rgba(79, 29, 149, 0.95), rgba(46, 16, 101, 0.95)); /* 💜 Dégradé violet clair → foncé */
    border: 1px solid rgba(216, 180, 254, 0.35); /* 💜 Bordure lilas */
    border-radius: 18px;
    padding: 2rem 1.75rem;
    box-shadow: 0 30px 45px -25px rgba(10, 1, 23, 0.9); /* 💜 Ombre plus sombre */
    text-align: left;
}

.quiz-card h2 {
    margin: 0 0 0.75rem;
    font-size: clamp(1.6rem, 3.5vw, 1.9rem);
    color: #f3e8ff; /* 💜 Titre du quiz plus clair */
}

.quiz-question {
    margin: 0 0 1.25rem;
    color: #ede9fe; /* 💜 Texte violet pâle */
    line-height: 1.5;
}

.quiz-options {
    display: grid;
    gap: 0.75rem;
    margin-bottom: 1.25rem;
}

.quiz-options button {
    border: 1px solid rgba(216, 180, 254, 0.3); /* 💜 Bordure lilas */
    border-radius: 12px;
    padding: 0.75rem 1rem;
    background: rgba(216, 180, 254, 0.08); /* 💜 Boutons légèrement teintés */
    color: #faf5ff; /* 💜 Texte clair */
    text-align: left;
    font-size: 1rem;
    cursor: pointer;
    transition: transform 0.15s ease, border-color 0.2s ease, background 0.2s ease;
}

.quiz-options button:hover {
    transform: translateX(4px);
    border-color: rgba(192, 132, 252, 0.6); /* 💜 Bordure violette au survol */
    background: rgba(192, 132, 252, 0.15); /* 💜 Fond un peu plus lumineux */
}

.quiz-options button:disabled {
    cursor: default;
    opacity: 0.9;
    transform: none;
}

.quiz-options button.is-correct {
    border-color: rgba(52, 211, 153, 0.8); /* ✅ Vert adouci */
    background: rgba(52, 211, 153, 0.2);
    color: #d1fae5;
}

.quiz-options button.is-incorrect {
    border-color: rgba(239, 68, 68, 0.8); /* ❌ Rouge plus vif */
    background: rgba(239, 68, 68, 0.2);
    color: #fee2e2;
}

.quiz-feedback {
    min-height: 1.5rem;
    margin: 0 0 1rem;
    font-weight: 600;
    color: #f9f9fb; /* 💜 Couleur de texte principale ajustée */
}

.quiz-feedback.positive {
    color: #34d399;
}

.quiz-feedback.negative {
    color: #f87171;
}

.quiz-continue {
    padding: 0.65rem 1.4rem;
    border-radius: 999px;
    border: none;
    font-weight: 700;
    background: linear-gradient(130deg, #a855f7, #7e22ce); /* 💜 Bouton violet dégradé */
    color: #fdf4ff; /* 💜 Texte clair */
    cursor: pointer;
    box-shadow: 0 18px 32px rgba(168, 85, 247, 0.38); /* 💜 Ombre assortie */
}

.quiz-continue:hover {
    transform: translateY(-1px);
}

.controls {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: center;
    align-items: center;
}

.controls button {
    padding: 0.6rem 1.4rem;
    border-radius: 999px;
    border: none;
    background: #c084fc; /* 💜 Boutons principaux lilas */
    color: #1e1b4b; /* 💜 Texte plus foncé pour contraster */
    font-weight: 700;
    cursor: pointer;
    transition: transform 0.15s ease, box-shadow 0.2s ease;
}

.controls button:hover {
    transform: translateY(-1px);
    box-shadow: 0 12px 20px -10px rgba(192, 132, 252, 0.7); /* 💜 Effet lumineux violet */
}

.controls button:active {
    transform: translateY(0);
    box-shadow: none;
}

.back-link {
    color: #fdf4ff; /* 💜 Liens plus clairs */
    text-decoration: none;
    font-weight: 600;
    padding: 0.3rem 0.6rem;
    border-bottom: 2px solid transparent;
    transition: border-color 0.2s ease;
}

.back-link:hover {
    border-color: #c084fc; /* 💜 Surlignage lilas */
}

.game-footer {
    text-align: center;
    margin-top: 1.5rem;
    color: #d8b4fe; /* 💜 Texte du footer lilas clair */
    font-size: 0.9rem;
}

@media (max-width: 600px) {
    .game-wrapper {
        padding: 2rem 1.25rem 1.5rem;
    }

    .controls {
        flex-direction: column;
    }
}
